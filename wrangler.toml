name = "interviewpad"
compatibility_date = "2024-09-01"
pages_build_output_dir = "dist"

# Set secrets via the dashboard or:
#   npx wrangler pages secret put OPENAI_API_KEY --project-name interviewpad
# Do NOT commit secrets to this file.
